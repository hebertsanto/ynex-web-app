export type FormData = {
    name: string,
    email: string,
    cep: string,
    address: string,
    tel: string,
}

export type Client = {
    _id:string,
    address: string,
    cep: string,
    email: string
    name: string,
    phoneNumber: string,
 }
